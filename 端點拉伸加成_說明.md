# 端點拉伸加成 — 完整說明

> 本文說明「端點拉伸加成」這個數學方法的核心概念、公式原理與應用範例。
> 目標讀者：具備基礎數學知識的高中生。你只需要知道「座標」、「函數」、「指數」這些概念就夠了。
>
> 本文件為 🎐 風鈴專案（wind chime simulator）的配套說明。
> 風鈴軟體 v0.11 將此方法用於風速因子的間隔調變（2^p 映射），
> 以及未來可能的音符權重分布設計。
> 搭配「端點拉伸演示.html」互動工具一起使用效果最佳。

---

## 1. 這個方法在做什麼？

想像你有一排士兵，他們原本等距站成一列：

```
0 ──── 0.25 ──── 0.50 ──── 0.75 ──── 1
```

現在你想要他們**往兩端靠攏**——站在兩邊的人不動，站在中間的人往外移。結果會變成：

```
0 ────    ──── 0.50 ────    ──── 1
```

中間的人好像被「推」出去了，這就是端點拉伸加成。

反過來，如果你想要所有人**往中間集中**，那就是負的 p 值做的效果。

---

## 2. 核心數學概念

### 2.1 歸一化

先把任何數值轉換到 0~1 的範圍，這個動作叫「歸一化」：

```
t = (x - min) / (max - min)
```

- 如果 x = min，t = 0
- 如果 x = max，t = 1
- 如果 x 是最中間的值，t = 0.5

我們所有的變換都只針對 t 來做，做完後再還原回原本的數值範圍。

### 2.2 不變點

所有方法都保證 t = 0.5 這個點**不動**。無論 p 是多少，中間點永遠保持在中間。

### 2.3 參數 p 的意義

p 是整個方法的控制參數，它的正負號決定方向、絕對值決定強度：

| p 的範圍 | 效果 | 直觀比喻 |
|-----------|------|----------|
| p > 1 | 往兩端**強烈拉伸** | 磁鐵把鐵粉吸到兩極 |
| p = 1 | **不變**（線性） | 什麼都沒發生 |
| 0 < p < 1 | 往兩端**弱拉伸** | 輕輕推了一下 |
| p = 0 | **全部集中到 0.5** | 所有人擠到中間一點 |
| p < 0 | 往**中間擠壓** | 彈力帶把兩端拉回中間 |

---

## 3. 三種方法詳解

### 方法一：V 形拉伸（最直覺）

**公式：**

```
如果 t < 0.5：
    t' = 0.5 × (2t)^|p|
如果 t > 0.5：
    t' = 1 - 0.5 × [2(1-t)]^|p|
```

**它是怎麼想的？**

把 0~1 這條線從 0.5 切開成左右兩半。左半從 0 到 0.5，右半從 0.5 到 1。

每一半各自做一個「冪次變換」（就是 t 的 p 次方），讓靠近 0 或 1 的值幾乎不變，靠近 0.5 的被大幅壓縮到兩端。

**p 的影響（以左半為例）：**

- `p = 0.5`：√t 的曲線，0.25 會變成 0.5×0.5 = 0.25（不動...咦？等等），實際上 `0.5×√(2×0.25) = 0.5×√0.5 ≈ 0.354`，比原來的 0.25 大了一點點，所以是**輕微往外推**。
- `p = 1`：`0.5×(2t) = t`，完全線性，不變。
- `p = 2`：`0.5×(2t)²`，0.25 變成 `0.5×(0.5)² = 0.125`，被往左推了。
- `p = 5`：0.25 變成 `0.5×(0.5)^5 = 0.0156`，幾乎被推到 0 了。

**當 p < 0（反轉方向）：**

公式變成：
```
如果 t < 0.5：
    t' = 0.5 - 0.5^|p| + t^|p|
如果 t > 0.5：
    t' = 0.5 + 0.5^|p| - (1 - t)^|p|
```

效果：靠近 0 和 1 的值被往內推（往 0.5 靠），原本在 0.5 附近的幾乎不變。

---

### 方法二：冪律偏移（最平滑）

**公式：**
```
t' = 0.5 + (t - 0.5) × |2t - 1|^|p|
```

**它是怎麼想的？**

`(t - 0.5)` 是離開中間的距離，正代表在右邊、負代表在左邊。
`|2t - 1|` 是離開中間的距離（0~1），中間是 0、兩端是 1。

把偏移量乘以 `(離開距離)^|p|`：
- 在端點（|2t-1|=1）：偏移保持不變 → 端點不動
- 在中間（|2t-1|=0）：偏移被乘 0 → 不動點還是 0.5
- 在中間偏一點的地方（|2t-1|很小）：偏移被大幅壓縮 → 被往外推（p>0）或往內拉（p<0）

**這個方法的優點：** 公式乾淨、全域一條公式、不需要分段。

**p < 0 時：**

公式變成 `t' = 0.5 - (t - 0.5) × |2t - 1|^|p|`
也就是把方向反過來——端點的值往中間移動。

---

### 方法三：反 Sigmoid（最平滑的曲線）

**公式：**
```
s = 1 / (1 + e^(-k(t - 0.5)))
t' = (s - s₀) / (s₁ - s₀)
```
其中 k = |p| × 3，s₀ 和 s₁ 是 t=0 和 t=1 時的 s 值，用來把結果重新拉到 0~1。

**它是怎麼想的？**

Sigmoid 函數是一條 S 曲線，原本用在機器學習中把任意數值壓到 0~1 之間。它的形狀是：中間陡峭、兩端平緩。

但我們要的是**相反**——中間平緩、兩端陡峭。所以我們先算 Sigmoid，然後把結果反過來：

- t=0.5 時 s=0.5 → 映射後還是 0.5（不動點）
- 靠近兩端時，s 曲線被拉伸得更開 → 數值被往兩端推
- p>0 時用正向映射，p<0 時做 1-t' 反轉

**這個方法的優點：** 曲線最平滑、沒有尖角。

---

## 4. 三種方法的比較

| 特性 | V 形拉伸 | 冪律偏移 | 反 Sigmoid |
|------|----------|----------|------------|
| 曲線平滑度 | 在 t=0.5 有尖角 | 平滑 | 最平滑 |
| 公式複雜度 | 簡單（需分段） | 最簡單（一條式） | 較複雜 |
| 端點行為 | 端點完全錨定 | 端點完全錨定 | 端點完全錨定 |
| p<0 效果 | 往中間擠壓 | 往中間擠壓 | 往中間擠壓（對稱反轉） |
| 直觀程度 | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ |

**建議：** 初學者先用**冪律偏移**，它公式最簡單、曲線平滑、效果直觀。

---

## 5. 實際應用範例

### 範例 1：成績分布調整

老師發現學生的期中考成績集中在 60~70 分，太密集了。他想讓成績拉開差距——高分的更高、低分的更低。

```
原始成績：55, 62, 65, 68, 71, 78, 85
歸一化後代入 p=2 的冪律偏移：
結果：    52, 58, 62, 68, 74, 82, 89
```

變成分數差距拉大了，高分的獲得加分獎勵、低分的不會被推到不及格太遠。

### 範例 2：風鈴敲擊分布

風鈴軟體中，有七根管子對應七個音。自然界的風不會均勻敲每一根——它更常敲到兩端的管子（極端風速時），中間的管子反而較少被敲到。

用冪律偏移 p=2：
```
管子編號：1  2  3  4  5  6  7
原始權重：等權重 1/7
拉伸後： 0.32 0.18 0.08 0.04 0.08 0.18 0.32
```
這樣第 1、7 根被敲到的機率遠大於第 4 根，更像真實的風。

### 範例 3：照片色調壓縮（負 p）

一張照片的亮度分布太極端——很亮和很暗的地方太多，中間調太少。你想把兩端的像素往中間壓。

用 p = -2 的冪律偏移，原本亮度 0.1（很暗）會變成約 0.34，亮度 0.9（很亮）會變成約 0.66，兩端往中間靠攏，畫面更柔和。

---

## 6. 小練習

1. 在演示工具中，切換到 V 形拉伸，p=2。觀察第 3 個點（t=0.105）和第 18 個點（t=0.895）各移動了多少？

2. 把 p 調到 -2，和 p=2 的移動方向有什麼不同？哪一端移動的幅度最大？

3. 為什麼三種方法在 p=1 時都會保持不變？試試看在演示中驗證。

4. 如果我想讓數值「稍微往外推但不要推到極端」，p 應該設定在什麼範圍？

---

## 7. 總結

- **端點拉伸**就是把原本等距的數值，透過非線性變換推向區間兩端（或拉向中間）
- **參數 p** 控制方向（正=兩端、負=中間）與強度（越大越極端）
- **三種方法**的數學形式不同但效果類似，冪律偏移最推薦
- **應用廣泛**：成績分布、風鈴模擬、影像處理、數據視覺化、音樂參數映射

> 數學並不遙遠——它就在你調整一個滑桿、看到點往兩邊飛散的那一刻裡。
